#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include "Array List.h"
int main() {
	List list;
	ListInit(&list);
	LInsert(&list, 1);
	LInsert(&list, 2);
	LInsert(&list, 3);
	LInsert(&list, 4);
	LInsert(&list, 5);

	if (LFirst(&list, &p1))

	return 0;
}